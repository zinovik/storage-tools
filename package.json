{
    "name": "storage-tools",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "backup": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/1-backup-json-files-to-local",
        "add": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/2-update-files-json.js",
        "rename": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/3-sync-storage-filenames",
        "upload": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/4-upload-files-to-storage",
        "clean": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/5-clean-up-storage-files",
        "start": "npm run backup && npm run add && npm run rename && npm run upload && npm run clean"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@google-cloud/firestore": "8.6.0",
        "@google-cloud/storage": "7.21.0",
        "google-auth-library": "10.7.0"
    },
    "devDependencies": {
        "stream-shift": "1.0.3"
    }
}
