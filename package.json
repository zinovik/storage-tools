{
    "name": "storage-tools",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "backupStorage": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/backup-storage-json-files-to-local",
        "rename": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/1-sync-storage-filenames",
        "upload": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/2-upload-files-to-storage",
        "clean": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/3-clean-up-storage-files",
        "cache": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/4-invalidate-cache",
        "start": "npm run rename && npm run upload && npm run clean && npm run cache",
        "backupFirestore": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/backupFirestore",
        "restoreFirestore": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/restoreFirestore"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@google-cloud/firestore": "8.6.0",
        "@google-cloud/storage": "7.21.0",
        "google-auth-library": "10.7.0"
    },
    "devDependencies": {
        "stream-shift": "1.0.3"
    }
}
