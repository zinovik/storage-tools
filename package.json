{
    "name": "storage-tools",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "backup:storage": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/0-backup-storage-json-files-to-local",
        "rename": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/1-sync-storage-filenames",
        "upload": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/2-upload-files-to-storage",
        "clean:files": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/3-clean-up-storage-files",
        "cache": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/4-invalidate-cache",
        "backup:firestore": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/0-backup-firestore-to-local-json-files",
        "clean:db": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/5-clean-up-firestore",
        "restore:firestore": "GOOGLE_APPLICATION_CREDENTIALS=key-file.json node src/0-restore-firestore-from-local-json-files",
        "start": "npm run backup:storage && npm run rename && npm run upload && npm run clean:files && npm run cache && npm run backup:firestore && npm run clean:db && npm run restore:firestore"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@google-cloud/firestore": "8.6.0",
        "@google-cloud/storage": "7.21.0",
        "google-auth-library": "10.7.0"
    },
    "devDependencies": {
        "stream-shift": "1.0.3"
    }
}
